# API 需求清单

> 说明：已遍历 `mobile/` 目录，当前未发现 Android/移动端的实际网络请求实现（如 Retrofit/OkHttp/Fetch 等）。因此本文件暂不包含具体 API 调用清单。待移动端实现网络层后，可按下方表格格式补充具体条目。

| 模块/功能 | API 路径 | 方法 | 请求参数 (Input) | 期望响应字段 (Output) | 前端调用位置 (可选) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| （暂无） |  |  |  |  |  |

建议对齐 Web 端的接口以便统一网关与鉴权策略（参考根目录 `WEB_API_REQUIREMENTS.md`）。当移动端开始接入登录/注册、排行榜、记录管理、管理员能力等功能时，可直接复用对应 API。鉴权建议统一使用 `Authorization: Bearer <token>`。

